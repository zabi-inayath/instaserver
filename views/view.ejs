<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=user.username%></title>
    <style>
      body {
        font-family: "Poppins", sans-serif;
        background-color: #f5f5f5;
      }

      .user-card {
        background-color: #fff;
        border-radius: 15px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        padding: 30px;
        margin: 20px auto;
        max-width: 450px;
        width: 80%;
        position: relative;
        transition: transform 0.2s ease-in-out;
      }

      .user-card h2 {
        margin-bottom: 20px;
        color: #333;
      }

      .user-card h2:first-child {
        font-size: 1.6em;
        font-weight: bold;
      }

      .user-card p {
        margin-bottom: 15px;
        color: #666;
      }

      .user-card .close-button {
        position: absolute;
        top: 10px;
        right: 10px;
        background-color: #f00;
        color: #fff;
        border: none;
        border-radius: 50%;
        padding: 10px 12px;
        font-weight: bold;
        cursor: pointer;
        transition: background-color 0.2s ease-in-out;
      }

      .user-card .close-button:hover {
        background-color: #d00;
      }

      .delete-btn {
        padding: 12px 25px; /* Adjust padding for better fit */
        background-color: #e74c3c; /* Reddish color */
        color: white;
        border: none;
        border-radius: 25px; /* Rounded corners */
        cursor: pointer;
        transition: background-color 0.2s ease-in-out;
        margin-top: 20px; /* Add some space from content */
        display: flex;
        justify-content: center;
        align-items: center;
        text-transform: uppercase; /* Convert text to uppercase for emphasis */
        font-weight: bold; /* Bold text for impact */
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1); /* Subtle shadow */
      }

      .delete-btn:hover {
        background-color: #c0392b; /* Darker red on hover */
      }

      .edit-btn {
        padding: 12px 25px; /* Adjust padding for better fit */
        background-color: #2669c7; /* Reddish color */
        color: white;
        border: none;
        border-radius: 25px; /* Rounded corners */
        cursor: pointer;
        transition: background-color 0.2s ease-in-out;
        margin-top: 20px; /* Add some space from content */
        display: flex;
        justify-content: center;
        align-items: center;
        text-transform: uppercase; /* Convert text to uppercase for emphasis */
        font-weight: bold; /* Bold text for impact */
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1); /* Subtle shadow */
      }

      .edit-btn:hover {
        background-color: #1c4174; /* Darker red on hover */
      }

      a {
        text-decoration: none;
      }
    </style>
  </head>
  <body>
    <div class="user-card">
      <h4>Username: <%=user.username%></h4>
      <p>Name: <%=user.name%></p>
      <p>Email: <%=user.email%></p>
      <p>Age: <%=user.age%></p>
      <p>Password: <%=user.password%></p>
      <p>Followers: <%=user.followers%></p>
      <p>Following: <%=user.following%></p>
      <button class="close-button">X</button>

      <form method="get" action="/ig/<%=user.id%>/edit">
        <button class="edit-btn">EDIT</button>
      </form>
      <form action="/ig/<%=user.id%>/reset">
        <button class="edit-btn">CHANGE PASSWORD</button>
      </form>

      <form method="get" action="/ig/<%=user.id%>/delete">
        <button class="delete-btn">DELETE</button>
      </form>
    </div>
    <script>
      let closeButton = document.querySelector(".close-button");
      closeButton.addEventListener("click", () => {
        window.location.href = "http://192.168.29.97:8000/ig/users";
      });
    </script>
  </body>
</html>
